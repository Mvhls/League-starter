<h1>Create a stat</h1>

<div class="row">
	<div class="span6 offset3">
		<%= form_for [@league, @stat_type] do |f| %>

			<%= f.label :name %>
			<%= f.text_field :name %>

			<%= f.submit "Create", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>