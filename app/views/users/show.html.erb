<% provide(:title, @user.name) %>
<div class="row">
	<aside class="span4">
		<section>
			<h1>
				<%= gravatar_for @user %>
				<%= @user.name %>
			</h1>
		</section>
	</aside>
</div>
		<div>
			<h2>Leagues</h2>
			<% @user.leagues.each do |league| %>
				<p><%= link_to league.name, league %></p>
			<% end %>
		</div>
	</div>
<div>